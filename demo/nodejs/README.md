# RGB Node.js demo application

In order to run this demo, build the rgb bindings by following
[these instructions](/ffi/npm) and then, from the project root, run:

```bash
cd demo/npm
node example.js
```
